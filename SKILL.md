---
name: literature-deep-reading-obsidian
description: 完整读取科学论文正文、Supporting Information 与配套附件，制作以逐图逐子图教学为核心、可直接放入 Obsidian 的中文深度文献笔记及 Figure/Source 文件夹。用于文献精读、作者思路复盘、主图和补图详解、证据边界审查与后续研究建议；不得退化为摘要、图注缩写或固定模板填空。
---

# 文献精读总控

## 目标与读者

扮演耐心、严谨的科研导师，服务于具备普通生物学基础、但第一次接触该细分领域的研究生。笔记的价值是节省读者阅读正文和反复检索概念的时间：读者应能沿着作者的科学问题逐步理解全部主图和补图，并知道证据能走到哪里。

专业性体现为把复杂问题讲清楚，不体现为术语密度、固定标签数量或总字数。事实准确、证据忠实和图片完整性始终优先。

正常执行时只读取三份活动规则，并按此顺序执行：

1. [references/user-iteration-overrides.md](references/user-iteration-overrides.md)：用户最终确认的成稿目标；
2. [references/source-ingestion-and-image-qa.md](references/source-ingestion-and-image-qa.md)：源文件读取、图片提取与客观验收；
3. [references/figure-teaching-depth-contract.md](references/figure-teaching-depth-contract.md)：模块二的逐图教学质量。

## 最终成品只有三个模块

### 返工现有笔记时的结构隔离

用户提供的旧笔记只能作为待核对的事实线索和失败样本，不能作为新版模块二的结构底稿。开始返工前先提取并保留用户明确认可的内容；其余旧模块二必须视为待替换区，禁止沿用旧的阶段标题、导航卡、固定问答、`读图前补课—逐面板—你可能还会问—Take-home` 套路或批量句序。不得只重写 Figure 1，再把旧笔记中的 Figure S1 以后原样接回。

如果用户先验收了一张 Figure 试写，该试写成为后续模块二的质量下限，而不是只供第一张图使用。每完成一张主图及其相邻补图，都要与已批准试写进行一致性比较：是否仍按实验单元组织；是否仍有具体视线轨迹和指标解释；是否仍保持自然叙事；是否又出现批量相同句序。后续可以因图型不同而改变讲法，但不能降低教学力度或换回另一套模板。

### 模块一：快速建立全文地图

用较短篇幅回答：

- 论文大致研究什么，现实或理论矛盾是什么；
- 作者依次提出哪些科学问题，为什么下一个问题会从前一个结果中产生；
- 每个问题大致采用什么方法解决，得到什么阶段答案。

模块一是导航，不提前逐图复述结果，不再建立庞大的术语库、八步阶段卡、重复证据地图或兼容索引。只有会立即妨碍理解主线的术语才在这里解释；其余概念在对应 Figure 附近就地说明。

### 模块二：用全部 Figure 把论文讲明白

这是成稿主体，通常投入约 65%–75% 的讲解精力，但比例不是字数门槛。按论文真实论证顺序放入全部主图、Scheme 和承重补图。Figure S 与主图采用相同的理解标准，不因位于 SI 而压缩。

模块二必须能够替代读者对 Results 正文的首次通读。执行者不能只翻译 legend 和描述肉眼变化，还要把正文中与该 Figure 直接相关的实验动机、设计选择、关键方法条件、作者解释和承上启下逻辑带到图旁。最终目标是：读者即使暂时不看论文正文，只按顺序阅读图片、Figure 前导和逐子图 Callout，也能完整说出论文做了什么、为什么这样做、证据如何一步步推进。这里的“替代首次通读”不意味着省略 Methods、Discussion 或原文核验，而是要求模块二本身形成可独立理解的证据故事。

模块二采用“实验还原—视觉带读—证据推理—新手复述”的内部工作法。它不是可见正文模板：不得把这四层写成每个 Callout 的固定标题。正式写面板前先恢复产生该图的实验；写作时再根据当前面板的真实难点，把必要内容自然讲出来。

每张 Figure 先用一小段建立共同实验背景：这组面板共同回答什么问题、有哪些重复出现的组别或技术。随后每个正式子图使用一个独立 Callout，例如：

```markdown
> [!note] Figure 1a｜这张图首先在给细胞分组
> ……自然讲解……
```

不使用逐面板表格。没有固定粗体标签，也没有必须按顺序出现的“为什么做—怎么做—预期—怎么看—边界”字段。相邻面板属于同一实验时，共同动机、分组和方法在 Figure 前导或首个面板讲清；后续面板只解释它新增的观察或证据，不机械重复。

为防止把教学压缩成高级图注，作者图注完整翻译与教学正文必须分成两个引用段落；图注译文本身不计入教学字数。每个正式子图的教学正文原则上不得少于 180 个中文字符；纯标签、单一流程箭头或紧邻代表图的机械定量可以更短，但必须在逐图门禁记录中说明共同内容由哪里承担。每张 Figure 的全部教学正文平均不得少于每面板 300 个中文字符，Figure 前导另不少于 250 个中文字符。候选筛选、机制转折、必要性、充分性、救援、撤销、通路分叉、互作及承重补图不得少于 500 个中文字符。这样用整图平均值允许简单面板短、复杂面板自然展开，不得为了命中三个固定档位把所有面板写成同样结构。

字数是最低防缩水门槛，不是合格充分条件。重复 legend、重复共同背景、堆砌术语、通用 P 值说明、通用“相关不等于因果”、换同义词复述同一结论，以及“先看哪里／悬而未决／证据边界”等轮换式提示语均不计入有效教学字数。达到字数后仍须通过面板特异性、新手复述和反套话检查。禁止为每个 Callout 预先分配“疑问句、视线句、结果句、边界句”再填内容；这仍是模板。

#### 每个子图真正要达到的效果

执行者先判断：“一个略有基础、但未读论文的人，最可能卡在这张子图的哪里？”围绕这个障碍自由组织讲解。读完后，读者原则上能够：

- 在原图中找到实际组别、颜色、区域、坐标、条带或数据点；
- 知道最合理的视线顺序，以及肉眼实际看到的方向；
- 明白当前数字、误差线、`n` 或统计标记在本图中意味着什么；
- 理解这张子图为什么出现在当前论证位置；
- 知道它新增了什么，以及不能独立推出什么。

这些是整体理解结果，不是五个必写段落。简单流程面板可以很短；承担机制定位、必要性、救援或竞争模型判别的面板可以很长。只写当前面板真正需要的内容。

#### 按图型改变讲法

- 流程或时间线：讲对象、先后顺序、唯一变化和最终读出。
- UMAP、热图、火山图和富集图：先解释每个点、行列、颜色和坐标，再解释为什么挑这些基因或通路，以及分析能否区分细胞状态、细胞组成和因果变化。
- WB：讲清泳道、蛋白行、内参、磷酸化/总蛋白关系以及应横向比较什么；不要把不同抗体条带深浅直接互比。
- IHC、IF、组织染色和电镜：说明颜色/结构对应什么、比较哪个组织区域、比例尺代表什么、代表图与定量图各新增什么。
- 柱图、散点图和曲线：说明横纵轴、每个点的真实统计单位、误差线与关键比较；P值不等于效应大小。
- co-IP、定位或互作图：区分同一复合物、空间共现、直接结合和功能因果。
- 动物模型与救援实验：讲清模型为何适合、对照承担什么任务、救援到底恢复了哪个层级。

以上只是路由提示，不是每张图的检查清单。

#### 背景知识按需就地补充

只补会直接消除当前读图障碍的知识，例如：

- 为什么选择当前基因、蛋白、细胞标志物或通路；
- 为什么几个指标要组合读取；
- 为什么设置当前模型、分组、对照、剂量或时间点；
- 指标升降在当前生物学问题中意味着什么。

这些是可选问题池，不是固定槽位。单纯流程图没有基因选择问题就不讲基因；同型对照重点解释它排除什么；定量面板若只是在验证代表图，不重复整套实验动机。也允许补充列表之外、更能帮助理解当前图的知识。

背景来源要分清：论文明确说明的写作“论文说明”；通用稳定知识可自然写作“背景补充”；根据实验顺序反推的目的标为“设计逻辑重建”。论文未说明且存在多种解释时，明确写“论文未说明，较合理的理解是……”，不得冒充作者原意。

#### 新缩写和新指标首次出现时立即解释

面向新手的正文不能先使用陌生英文缩写、基因/蛋白名、评分、实验技术或功能指标，再指望读者自行检索。每个新术语第一次出现时，就地给出足以继续读图的简短解释：中文名称；英文全称或缩写来源（能够可靠确认时）；它是什么；在当前实验中代表哪个生物学过程；通常怎样读取升高、降低或阳性变化。模型和技术还要说明它解决什么问题，评分还要说明高低方向，功能指标还要说明数值变化意味着功能改善还是受损。

例如不能只写“检测 ACAN、MMP13、OCR 和 OARSI”。应让读者第一次看到时即知道：ACAN 是聚集蛋白聚糖、代表软骨基质合成/保存；MMP13 是能分解软骨胶原的基质金属蛋白酶；OCR 是耗氧率、用于观察线粒体呼吸；OARSI 是骨关节炎组织学评分且分数越高通常表示软骨损伤越重。这里只示范解释颗粒度，不要求固定句式。

同一术语在全文首次解释后可以直接使用，不必反复展开。若一张热图或通路图首次集中出现大量基因、缩写或指标，可在对应 Callout 内用紧凑列表或短段落建立“本图术语钥匙”，但不得跳过并让读者另行搜索。无法可靠确认全称或生物学方向时回查正文、Methods、SI 或权威来源；仍不确定则明确标注，禁止凭印象扩写。

缩写解释不能只照顾论文核心分子。染料、内参、统计方法、动物品系、载体、缓冲液、单位和数据库编号等，只要新手可能不知道，也在首次出现时用一句话说明其类别和当前用途，例如 DAPI/DAB、PBS、WT、C57BL/6、kDa、FPKM。英文全称很长时可给中文名称加简短用途，但不能只留下裸缩写。

#### 作者图注必须完整

每个正式子图必须承载原始 legend 对应该面板的完整中文翻译。共同适用于多个面板的样本量、统计方法、比例尺、缩写和显著性说明，可在 Figure 前导或首个相关 Callout 完整翻译一次并明确适用范围，不在每个面板重复。

必须清楚区分作者图注或正文明确陈述、执行者从图中观察到的内容、背景补充、设计逻辑重建与证据边界。不得把完整 legend 压缩成“某指标比较”“某染色与评分”，也不得根据图像替作者补写原话。无法可靠辨认时回到 PDF、正文、Methods 和 SI；仍无法确认则标记来源不足。

#### 深度来自解决真实疑问

复杂面板应主动回答读者最自然的疑问，例如：为什么作者用这些基因代表某个生物过程；为什么代表图之后还要定量；为什么多条通路都变化后只重点追踪其中一条；为什么抑制剂只能说明参与而不能证明直接上下游。

并非每个面板都必须提出问题。判断标准是：如果一个重要疑问不回答，读者是否仍需要自行查资料或重建作者逻辑。若是，就补；若否，就不加。

#### 深度按整张 Figure 的信息闭合度验收

单个面板可以只承担一个很小的教学任务，但一整张 Figure 及其相邻承重补图必须共同讲完这一轮实验。读完后，读者应能用自己的话复述：作者此前卡在哪里；本轮实验总体怎样破解这个问题；不同子图各自贡献了什么而非彼此重复；实际结果把结论推进到哪一层；还留下什么问题，以及下一张 Figure 为什么会出现。

这些问题是对整张 Figure 的复述结果，不是要求每个 Callout 依次写六个栏目。共同背景可以集中在 Figure 前导，关键设计可以放在最适合的首个面板，定量和边界可以放在真正承载它们的面板，过渡可以写在整图结尾。只要有一个承重环节在整张 Figure 中无处可找到，导致读者必须回正文才能补全实验逻辑，该 Figure 就仍然偏薄。

补图同样参与信息闭合：若 Figure S 提供主图未展示的敲低效率、阴性对照、剂量依据、原始分组、独立重复或关键排除证据，必须在对应位置解释它具体托住了主图哪一句结论，不能只说“支持主图”。

#### 先还原实验，再解释面板

面板字母不是科学逻辑。正式写一张 Figure 前，先在内部把它拆成真实实验单元，并明确：作者当时缺少哪项证据；实验改变了什么、保持了什么；每个组分别承担疾病基线、阴性对照、阳性对照、干预或救援中的什么作用；测量的真正对象和统计单位是什么；这套设计原本希望区分哪些可能解释。

随后确定每个面板在同一实验中的分工。例如，流程图交代处理顺序，代表图提供空间或形态证据，定量图判断变化是否跨样本成立，功能读出判断分子变化是否产生生物学后果，救援或撤销实验提升因果等级。共同内容在实验单元起点讲一次，不能让每个面板各自变成孤立摘要。

#### 真正“带着眼睛看图”

对中、高负荷面板，讲解必须能让读者不依赖作者结论亲自完成观察。根据图型自然说明：图上有哪些对象；从哪一组或哪一区域开始；接着比较什么；肉眼实际看到的差异是什么；代表图、单个数据点、均值、误差线和统计标记各自提供哪一层信息。先陈述观察，再解释意义，不能一上来只给结论。

组别多时要把缩写翻译成实验问题，并说明哪些比较有效、哪些跨组或跨蛋白比较属于误读。染色图要教组织方向、颜色、结构、倍率和定量对象；WB 要教泳道、目标蛋白行、内参以及同一蛋白的横向比较；组学图要教点、轴、颜色、阈值和候选筛选逻辑。讲解深度以读者能否在原图中复现观察路径为准。

图中出现的文字必须全量入账并得到说明，包括面板字母、标题、组名、处理条件、时间点、剂量、样本或细胞标签、横纵轴、单位、图例、颜色条、缩写、基因/蛋白名、箭头或框选标注、比例尺、`n`、误差线定义、显著性符号和图内脚注。说明不能只做中英互译，还要告诉新手这段文字在当前图中标记什么、控制哪项比较或怎样影响读法。重复适用于多个面板的文字可在 Figure 前导或首次出现处统一解释，并明确覆盖范围；不得为了避免重复而完全漏掉。无法辨认的文字必须回看高分辨率 PDF 或扩大图片，仍无法确认则明确记录，不能猜测。

热图、点图、通路图或大规模标志物图中的文字同样不能以“数量太多”为由略过。可以按共同生物学功能、细胞身份或实验角色分组解释，而不必把每个名称写成同样长的段落；但图上每个标签都必须能在这些分组说明中找到归属。重点是让读者理解作者为何用这一组标签支撑命名或筛选，而不是制作与读图无关的百科全书。

讲解只能要求读者观察最终图片中实际存在的元素。写“先确认敲低/过表达是否成功”“先看阳性对照”等之前，必须核实对应条带、组别或标记确实出现在当前面板；若设计上应该存在但图中没有，直接写成报告缺口，并指出需要由哪个补图、Methods 或额外实验承担。不得把预期设计误写成已经展示的观察。

#### 把数字翻译成生物学意义

不能停在“P<0.05”或“显著升高”。在当前面板确实涉及定量时，说明每个点代表什么、`n` 的实验单位、中心趋势和误差线、核心比较、变化方向与大致幅度；原图未给效应量或可信区间时明确无法判断精确幅度。统计显著性只说明观察与零差异模型的相容程度，不自动等于效应大、机制成立或临床重要。

#### 把观察、推理和推进分开

每个核心实验都要区分：图中直接观察到了什么；作者据此提出什么解释；该解释依赖哪些前提；替代解释是否仍在；这一结果为何改变下一步实验选择。证据边界必须针对当前设计，例如细胞组成混杂、药物外靶、同一复合物不等于直接结合、代表图缺少量化等，禁止批量附加“相关不等于因果”。

禁止以下做法：

- 给所有 Callout 批量填同一组粗体标签；
- 每张面板都重复阶段背景、双向预期、通用 P 值解释或“相关不等于因果”；
- 用统一句式把短面板扩写到固定字数；
- 图像面板和紧随其后的定量面板写成同义复述；
- 补图只给一句结论，或连续出现“压缩版＋扩写版”重复说明；
- 在模块二之外再写一套逐实验阶段正文，造成双重叙事。
- 以没有粗体标签为理由，跨大量面板重复“实验中—判别逻辑—读图时—图中可见—证据边界”等相同句序；无标签的固定问卷仍是固定模板。
- 每张 Figure 系统性附加相同的“读图前补课”“看完你可能还会问”和“Take-home message”三件套；这些内容仅在当前图确实需要时自然出现，不能成为流水线包装。

### 模块三：集中复盘

用相对简洁的篇幅总结论文真正建立的稳健贡献、最值得学习的设计、主要技术/统计/因果局限、最关键的未闭合箭头或竞争解释，以及最值得优先做的下一步实验。

模块三不重复逐图内容，不要求固定数量的竞争模型、固定字数或大型审计表。复杂机制论文可以适当展开，但重点仍在模块二。

## 来源读取与写作许可门

1. 根据题目、作者、DOI、期刊和正文内容确认正文、SI 和版本对应关系，不根据文件名猜测。
2. 每份 PDF、SI 和承重附件读取至 EOF；工具截断时缩小范围并重叠续读。
3. 建立内部页面、Figure、实验分组、方法、命题证据和 SI 台账；台账不进入最终目录。
4. 将正文、Methods、legend 和 SI 交叉核对关键对象、剂量、时序、`n`、统计单位与结果。
5. 所有源文件读至 EOF、全部图表入账、关键异常处理完毕后，才开始一次性写最终成稿。不得先按摘要和主图写一版再补 SI。

## 模块二采用隔离任务的单 Figure 工作流

内部串行不足以证明逐图验收，因为同一个执行上下文可能先生成全文，再倒推每张 Figure 都已通过。默认采用“一条用户命令、内部自动编排”的模式：主任务完成来源核对和证据台账后，按 Figure 顺序逐一启动隔离的子任务；每个子任务只允许制作一张物理 Figure，主任务验收通过后才启动下一张。用户无需逐张发送命令。

Figure 子任务必须严格串行，禁止为了提速并行生成多张图。每个新子任务只接收当前 Figure 的原图、完整 legend、对应 Results、必要 Methods/SI、全文术语状态和第一张已批准稿作为质量标尺；不得接收其他未完成 Figure 的写作骨架。当前子任务失败时，只向该子任务发送具体返工要求或重新启动当前 Figure，不得越过它开始下一张。

### 自动编排模式（完整论文默认）

用户一次提出“完整处理论文”即可启动。主任务依次执行：准备当前 Figure 证据包 → 启动一个只负责当前 Figure 的隔离子任务 → 保存独立稿 → 主任务逐项门禁 → 未通过则返工当前稿 → 通过后更新状态 → 再启动下一 Figure。所有 Figure 完成后，主任务自动进入最终拼接，不需要用户再发送命令。

主任务不得亲自一次性撰写所有 Figure，也不得同时把多个 Figure 分给并行子任务。隔离子任务完成一张图后必须结束；下一张使用新的任务上下文，以避免前一张的段落骨架扩散。若当前环境不支持隔离子任务或可验证的任务级分离，必须停止并向用户说明，不能退回单上下文全文生成后声称执行了逐图门禁。

### 手动逐图模式（用户明确要求时）

若用户明确说“只做 Figure X”或希望逐张查看，则一次用户命令只处理被点名的一张物理 Figure。只建立当前 Figure 的证据包，只写当前 Figure 的前导和全部子图 Callout，只审核当前 Figure；完成后停止等待用户。

每张独立稿保存到最终目录之外、可跨任务保留的同级工作目录 `_figure_drafts/<论文短标识>/`，文件名为 `Figure-1.md`、`Figure-S1.md` 等。独立稿包含对应图片链接、完整图注翻译、教学正文和本图必要的局部过渡，但不包含模块一、模块三或其他 Figure。返工命令只能修改被点名的 Figure 文件。

每张 Figure 必须依次经历五个状态：`证据包完成 → 逐子图写作完成 → 图中文字全量核对完成 → 不看正文的新手复述通过 → 本图 PASS`。PASS 只对当前独立文件生效。完成后向用户报告文件路径、本图面板数和验收结果，然后硬停止，不能自动继续下一张。

### 最终拼接

自动编排模式在全部 Figure 通过后自动进入拼接；手动逐图模式则等待用户明确发送“拼接”或同义命令。先盘点原文和 SI 的 Figure 清单，与 `_figure_drafts` 中的独立稿逐一对应；任何 Figure 缺失或未通过，停止拼接并列出缺口。全部齐全后按论文论证顺序原样合并已验收正文，仅允许统一标题层级、图片路径、术语一致性、重复共同背景和 Figure 间的自然过渡；禁止把逐图正文重新摘要、压缩、改写成统一模板或为了全文长度删减内容。随后补写简洁的模块一和模块三并执行最终审计。

逐图门禁必须核对该图的原始 legend、对应 Results、必要 Methods、承重 SI、最终图片和全部可见文字。逐个面板确认：完整图注没有缩写成标签；图注与教学正文已经分开；扣除图注和套话后的有效教学字数达到相应负荷下限；首次术语已经解释；读者能在图中找到所述对象；实际观察、数字意义、设计逻辑和证据推进足以让新手复述；不存在标题截断、同段重复或图注与正文的样本量/剂量/时间矛盾。任一承重面板不合格，整张 Figure 不得 PASS。

在 `_figure_drafts/<论文短标识>/gate-status.json` 保留跨任务逐图门禁记录，至少记录 Figure 名、文件路径、面板范围、证据来源位置、可见文字核对结果、首次术语核对结果、新手复述结果、发现的问题、返工次数和最终 PASS 状态。最终拼接并通过总审计后删除整个 `_figure_drafts` 工作目录，不进入最终成品。

从第二张 Figure 起，每次验收都把当前独立稿与用户批准稿或第一张通过的 Figure 并排检查质量漂移。发现共享句首、固定段落顺序、解释密度突然下降或 legend 开始缩水时，当前 Figure 不得 PASS。自动编排模式在所有 Figure 分别通过后直接拼接；手动逐图模式等待用户命令。

## 图片提取与视觉验收

提取全部主图、Scheme 和 Figure S，优先使用原始对象；不可靠时使用约 300 dpi 页面渲染。最终图片必须完整保留面板标签、全部子图、坐标、图例、比例尺、统计标记及跨页面板。

对每张图执行：

1. 从 legend 建立面板范围台账；
2. 将最终 PNG 与原始页面并排检查；
3. 从笔记中的子图 Callout 反查图片，确保双向对应；
4. 无法百分之百确认裁切完整时，回退到完整页面；完整优先于美观。

文件存在、链接可用和像素内容完整是不同验收项，不能互相替代。

## 正式 Table 与其他材料

正文或 SI 的正式 Table、参数表、资源表和数据清单需要完整翻译表题、列名、可读单元格、单位、脚注、缩写和统计符号。允许拆分宽表，但不得只摘关键行。Methods、统计说明、Data availability 和承重 SI 文字放在最相关的位置或模块三，不用“详见 SI”跳过。

## 质量验收

### 自动审计检查客观完整性与明确禁止的系统性退化

`scripts/audit_note.py` 把以下客观问题设为阻断：最终目录结构错误、源文件或图片缺失、链接断裂、重复子图标签、明显占位符、Markdown/Callout 语法损坏、未进行全量图片视觉核验，大量面板重复使用同一组教学字段，以及大规模出现“作者图注主导＋极短附加解释”的批量压缩。

审计还要检测跨面板重复的句首骨架，而不能只匹配预先列出的关键词。多个相同句首若覆盖大比例面板，即使执行者把旧标签改成同义词，也属于系统性模板污染并阻断交付。

孤立出现“为什么做”之类的自然措辞不是错误；只有它们以标签形式跨大量面板机械重复时才阻断。单个 Callout 的长度仍不是门槛；但在面板很多的复杂论文中，若绝大多数 Callout 都由“作者图注”开头且整体极短，这是可观察的批量摘要退化，应阻断交付。阶段数量、是否写出“双向预期”和个别短面板只能作为人工提示。禁止用机械补字消除错误，必须按真实教学负荷重写。

### 人工教学审查才判断深度

抽样只用于快速发现问题，不能作为最终通过依据。最终必须检查全部高负荷面板与全部承重 Figure S；具体执行 [references/figure-teaching-depth-contract.md](references/figure-teaching-depth-contract.md)。只读 Figure 前导和相应 Callout，模拟新手复述：图上比较谁、测什么、先看哪里；实际结果及数字含义；为什么选这些对象或分组——仅在这确实是理解障碍时；该图新增什么、不能说明什么。

如果复述仍需要查外部资料、猜缩写、猜指标方向或自己重建实验逻辑，则返工整张 Figure。再执行替换测试：若把面板编号和专有名词换掉，文字仍可套到多张图，说明解释过于模板化。

另外执行“图中文字覆盖检查”：从最终 PNG 逐项抄录所有可见文字，与 Figure 前导和 Callout 反向对应。每一项都必须在正文中被翻译或定义，并说明其读图作用；共享项可以集中解释一次。任何未解释的坐标、单位、组名、缩写、颜色图例、比例尺或统计符号都会迫使新手猜图，整张 Figure 不得通过。

再执行“图中存在性检查”：逐句核对所有“先看、确认、可见、显示”等观察性表述，确保所指对象确实位于当前图片。图中缺失的操控效率、对照或读出必须明确称为缺口，不能借用实验设计常识补进画面。

人工验收追求清楚和面板特异性，不追求项目齐全。不得以总字数、Callout 数量、章节数量或脚本 PASS 代替阅读体验。

完成全部逐图讲解后，再执行一次“无正文通读测试”：隐藏模块一、模块三和论文正文，只按顺序阅读模块二。模拟读者应能复述全文科学问题如何变化、每轮实验改变了什么证据状态、核心机制由哪些图共同建立，以及作者最终仍未解决什么。若只能复述一串图片结果，却说不出为什么做和如何推进，说明模块二深度不足；若必须依赖模块一或模块三补全主线，也说明模块二尚未独立成立。

## 最终目录

```text
YY.MM.DD-准确的论文中文题目/
├── 设计思路与证据审查.md
├── Figure/
└── Source/
```

目录日期使用创建笔记时的本地日期；frontmatter 的 `published` 记录论文发表日期。最终目录不得包含 OCR、渲染页、脚本、日志、台账、缓存或 contact sheet。最终只报告目录、核心 Markdown、图片数、Source 数和未解决异常。
